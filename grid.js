

console.log("TEST");


// var drawGrid = (canvas, ctx, tileSize) => {
//     for (let r = 0; r < canvas.width / tileSize; r++) {
//         for (let c = 0; c < canvas.height / tileSize; c++) {
//             const tileNum = c + canvas.width / tileSize * r;

//         }
//     }
// };